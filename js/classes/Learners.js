export default class Learners {

    constructor(learner) {
        this.id = leaners.id;
        this.first_name = learner.first_name
    }

    getIdLearner() {
        return this.id
    }

    getName() {
        return this.first_name
    }
}